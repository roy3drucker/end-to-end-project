kind: Service
apiVersion: v1
metadata:
  name: {{ .Release.Name }}
  labels:
    app: {{ .Release.Name }}
spec:
  selector:
    app: {{ .Release.Name }}
  type: {{ .Values.serviceType }}
  ports:
  - name: http
    protocol: TCP
    port: {{ .Values.port }}
    targetPort: {{ .Values.port }}
